server {
    listen 80;
    root /srv/macos-recovery;
    autoindex on;
    autoindex_format json;
    sendfile on;
    tcp_nopush on;
    # BaseSystem.dmg is ~0.7-3 GB; let clients resume
    add_header Accept-Ranges bytes;
    location / { try_files $uri $uri/ =404; }
}
