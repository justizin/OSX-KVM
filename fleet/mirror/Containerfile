# Static HTTP mirror of macOS recovery images for fetch-macOS-v2.py.
# Layout served at /: <product id>/BaseSystem.dmg, <product id>/BaseSystem.chunklist, index.json
FROM nginx:1.27-alpine
COPY nginx.conf /etc/nginx/conf.d/default.conf
VOLUME ["/srv/macos-recovery"]
EXPOSE 80
